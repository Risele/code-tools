# C-tools

Collection of C lang tools

## fast_math.h

Fast bit-magic calculations of basic math functions (1/x, sqrt(x), 2^x, 1/sqrt(x)...)

## uint_strings.h

Macro to initialize constant uint_t array[] that store strings with ... strings. 
